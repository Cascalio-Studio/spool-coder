"""
Test package for spool-coder security functionality
"""

# Add the src directory to the path so we can import modules
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))